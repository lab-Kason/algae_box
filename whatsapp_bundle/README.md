# WhatsApp bundle

Reassemble on Mac or Windows:

```bash
cat algae_box_portable.tar.gz.part* > algae_box_portable.tar.gz
# then unpack

tar -xzf algae_box_portable.tar.gz
```

Order matters: send `part01` first, then `part02`.
